{#
/**
 * @file
 * Template for a node news full content top kiosk.
 */
#}
{% include 'artisan_caminofrances:town-hall-content-top-kiosk' with {
  slides: node.hasField('field_kiosk_slides') and not node.get('field_kiosk_slides').isEmpty() ? content.field_kiosk_slides : false,
} %}
